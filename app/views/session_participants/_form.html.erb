<%= form_with(model: session_participant) do |form| %>
  <% if session_participant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(session_participant.errors.count, "error") %> prohibited this session_participant from being saved:</h2>

      <ul>
        <% session_participant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.number_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
